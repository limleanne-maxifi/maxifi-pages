---
pagination:
  data: demos
  size: 1
  alias: demo
permalink: "/demo/{{ demo.slug }}/index.html"
eleventyExcludeFromCollections: true
---
{#-
  Generic AI-visibility demo template — one page per demos/{slug}.json config.
  Adding a prospect = adding one config file; this template carries zero
  client-specific strings. The live tester reuses the same /demo/probe proxy
  (netlify.toml → Render ai-visibility-engine) as the original demo page, so no API keys
  ever reach the client. Sections, in conversion order: hero+ticker · explainer
  video · live test (email gate at the reveal) · MAXIFI calculator · proof ·
  ladder · method note · final dual CTA.
-#}
<!doctype html>
<html lang="en-GB">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="robots" content="noindex">
<title>{{ demo.client_name }} — what AI tells your buyers, live · Maxifi Digital</title>
<meta name="description" content="Live AI-visibility demonstration for {{ demo.client_name }}: see word-for-word what ChatGPT, Claude, Gemini, Perplexity and Google AI Overviews tell buyers — and what that visibility is worth.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400&display=swap">
<style>
  /* ── House style: Inter · blue #1D4ED8 · ink #16181D · off-white #FBFAF6 ── */
  :root{
    --sans:"Inter",-apple-system,BlinkMacSystemFont,"Segoe UI",system-ui,sans-serif;
    --primary:#1D4ED8; --primary-hover:#1E40AF; --primary-active:#1E3A8A; --on-primary:#ffffff;
    --canvas:#FBFAF6; --layer:#ffffff; --field:#ffffff;
    --hairline:#E7E4DC; --border-strong:#CFCBC0;
    --ink:#16181D; --ink-muted:#4B4F58; --ink-subtle:#71757E; --placeholder:#A5A8AF;
    --error:#B42318; --dark:#16181D; --container:1056px;
    --cited-fg:#0e6027; --cited-bg:#d9f0e0;
    --partial-fg:#7d4e00; --partial-bg:#fdf0cd;
    --missing-fg:#7a1521; --missing-bg:#fbdcde;
  }
  *{box-sizing:border-box}
  [hidden]{display:none!important}   /* keep `hidden` winning over display:flex/grid rules */
  html{scroll-behavior:smooth}
  body{margin:0;background:var(--canvas);color:var(--ink);font-family:var(--sans);
    font-size:16px;line-height:1.5;-webkit-font-smoothing:antialiased;font-weight:300}
  a{color:inherit}
  .mx-container{max-width:var(--container);margin:0 auto;padding:0 32px}
  @media(max-width:680px){.mx-container{padding:0 20px}}
  .mx-section{padding:56px 0}
  .mx-eyebrow{font-size:11px;letter-spacing:.48px;text-transform:uppercase;color:var(--primary);
    font-weight:600;display:flex;align-items:center}
  .mx-acc-line{display:inline-block;width:24px;height:1px;background:var(--primary);margin-right:12px}
  .mx-display{font-weight:300;font-size:clamp(40px,6vw,72px);line-height:1.05;letter-spacing:-.02em;margin:0}
  .mx-h2{font-weight:300;font-size:clamp(28px,3.4vw,44px);line-height:1.1;letter-spacing:-.012em;margin:0}
  .mx-italic{font-style:italic}
  .mx-body{font-size:16px;line-height:1.55;color:var(--ink-muted)}
  .mx-meta{font-size:12px;letter-spacing:.02em;color:var(--ink-subtle);text-transform:uppercase}
  .mx-num{font-variant-numeric:tabular-nums}
  .mx-btn{display:inline-flex;align-items:center;gap:10px;font-size:14px;font-weight:400;
    padding:13px 20px;border-radius:0;border:1px solid transparent;background:var(--primary);
    color:var(--on-primary);cursor:pointer;text-decoration:none;white-space:nowrap;
    transition:background 240ms cubic-bezier(.2,.6,.3,1)}
  .mx-btn:hover{background:var(--primary-hover)}
  .mx-btn:active{background:var(--primary-active)}
  .mx-btn.is-secondary{background:transparent;color:var(--primary);border-color:var(--primary)}
  .mx-btn.is-secondary:hover{background:rgba(29,78,216,.08)}
  .mx-btn.is-on-dark{background:#fff;color:var(--primary)}
  .mx-btn.is-sm{padding:9px 16px;font-size:13px}
  .mx-btn:disabled{opacity:.6;cursor:not-allowed}
  .mx-card{background:var(--layer);border:1px solid var(--hairline);border-radius:0;padding:24px}
  .cta-row{display:flex;gap:12px;flex-wrap:wrap}
  /* header — sticky, with site-wide booking CTA */
  .c-header{position:sticky;top:0;z-index:50;background:rgba(251,250,246,.92);
    backdrop-filter:saturate(140%) blur(12px);-webkit-backdrop-filter:saturate(140%) blur(12px);
    border-bottom:1px solid var(--hairline)}
  .c-header .bar{display:flex;align-items:center;justify-content:space-between;padding:12px 0;gap:12px}
  .mx-logo img{display:block;height:40px;width:auto}
  /* ticker */
  .ticker{display:flex;align-items:stretch;overflow:hidden;background:var(--layer);
    border-bottom:1px solid var(--hairline)}
  .ticker-tag{flex:none;display:inline-flex;align-items:center;gap:9px;background:var(--ink);color:#fff;
    font-size:11px;font-weight:300;letter-spacing:.5px;text-transform:uppercase;padding:0 18px;white-space:nowrap}
  .ticker-dot{width:7px;height:7px;border-radius:50%;background:#42be65;animation:tkPulse 2s ease-out infinite}
  .ticker-viewport{position:relative;flex:1;overflow:hidden;
    -webkit-mask-image:linear-gradient(90deg,transparent,#000 5%,#000 95%,transparent);
    mask-image:linear-gradient(90deg,transparent,#000 5%,#000 95%,transparent)}
  .ticker-track{display:inline-flex;align-items:center;white-space:nowrap;padding:9px 0;
    will-change:transform;animation:tickerScroll 46s linear infinite}
  .ticker:hover .ticker-track{animation-play-state:paused}
  .tk-set{display:inline-flex;align-items:center}
  .ticker-item{display:inline-flex;align-items:center;gap:10px;font-size:12.5px;color:var(--ink-muted);
    padding:0 6px;white-space:nowrap}
  .ticker-item .tk-q{font-style:italic}
  .ticker-item .tk-cite{display:inline-flex;align-items:center;gap:5px;color:#0e6027;letter-spacing:.2px}
  .ticker-item .tk-miss{display:inline-flex;align-items:center;gap:5px;color:var(--missing-fg);letter-spacing:.2px}
  .ticker-sep{color:var(--border-strong);font-size:8px;padding:0 16px}
  @keyframes tickerScroll{from{transform:translateX(0)}to{transform:translateX(-50%)}}
  @keyframes tkPulse{0%{box-shadow:0 0 0 0 rgba(66,190,101,.5)}70%,100%{box-shadow:0 0 0 6px rgba(66,190,101,0)}}
  /* hero */
  .hero{position:relative;overflow:hidden;padding:48px 0 54px}
  .hero-glow{position:absolute;top:-160px;right:-120px;width:460px;height:460px;border-radius:50%;
    background:radial-gradient(closest-side,rgba(29,78,216,.08),transparent 70%);pointer-events:none}
  .sector-chip{display:inline-block;font-size:11px;letter-spacing:.06em;text-transform:uppercase;
    color:var(--ink-muted);border:1px solid var(--hairline);background:var(--layer);padding:5px 10px;margin-bottom:18px}
  /* video */
  .video-shell{margin-top:30px;position:relative;background:var(--dark);border:1px solid var(--hairline);
    aspect-ratio:16/9;max-width:880px}
  .video-shell iframe{position:absolute;inset:0;width:100%;height:100%;border:0}
  .video-poster{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;
    justify-content:center;gap:14px;color:#fff;text-align:center;padding:24px;cursor:pointer;border:0;
    background:radial-gradient(closest-side at 50% 40%,rgba(29,78,216,.35),rgba(22,24,29,.97));width:100%}
  .video-poster .play{width:64px;height:64px;border-radius:50%;background:#fff;color:var(--primary);
    display:flex;align-items:center;justify-content:center;font-size:22px;padding-left:4px}
  .video-poster h3{margin:0;font-weight:400;font-size:20px}
  .video-poster p{margin:0;font-size:13px;color:#c9cbd1;max-width:46ch}
  .cap-note{margin-top:10px;font-size:12px;color:var(--ink-subtle)}
  /* live test */
  .panel{margin-top:30px;background:var(--layer)}
  .row{display:grid;grid-template-columns:1fr 1.5fr;gap:18px}
  @media(max-width:680px){.row{grid-template-columns:1fr}}
  .fld{display:block;font-size:12px;color:var(--ink-muted);margin:0 0 6px}
  input[type=text],input[type=email],input[type=number]{width:100%;padding:11px 12px;
    border:1px solid var(--border-strong);border-radius:0;font-family:var(--sans);font-size:14px;
    color:var(--ink);background:var(--field)}
  input::placeholder{color:var(--placeholder)}
  input:focus{outline:none;border-color:var(--primary);box-shadow:0 0 0 2px rgba(29,78,216,.15)}
  .presets{margin:16px 0 2px;display:flex;flex-wrap:wrap;gap:8px}
  .chip{font-size:11px;letter-spacing:.04em;border:1px solid var(--border-strong);background:transparent;
    color:var(--ink-muted);border-radius:0;padding:6px 12px;cursor:pointer;font-family:var(--sans)}
  .chip:hover{border-color:var(--primary);color:var(--primary)}
  .actions{display:flex;align-items:center;gap:16px;margin-top:20px;flex-wrap:wrap}
  .safe{display:inline-flex;align-items:center;gap:8px;font-size:13px;color:var(--ink-muted);cursor:pointer}
  .safe input{width:auto;cursor:pointer;accent-color:var(--primary)}
  .tokwrap{margin-left:auto;display:flex;align-items:center;gap:8px}
  .tokwrap input{padding:8px 10px;font-size:13px;width:180px}
  .banner{margin-top:16px;padding:12px 14px;font-size:14px;background:#fdf0ef;color:#912018;
    border-left:3px solid var(--error)}
  .notice{margin-top:16px;padding:12px 14px;font-size:14px;background:#fdf7e7;color:#684e00;
    border-left:3px solid #f1c21b}
  .samplebar{margin-top:16px;padding:12px 14px;font-size:12px;font-weight:600;letter-spacing:.02em;
    background:#fdf7e7;color:#684e00;border-left:3px solid #f1c21b}
  .runmeta{margin-top:26px;font-size:12px;letter-spacing:.04em;text-transform:uppercase}
  .runmeta.live{color:#0e6027}
  .runmeta.sample{color:#8e6a00}
  .summary{margin-top:22px;display:flex;align-items:center;gap:22px;border:1px solid var(--hairline);
    background:var(--field);padding:20px 24px}
  .summary .score{font-weight:300;font-size:40px;min-width:84px;text-align:center;letter-spacing:-1px;
    color:var(--primary);font-variant-numeric:tabular-nums}
  .summary.s0 .score{color:var(--error)} .summary.s1 .score{color:#8e6a00}
  .summary.s2 .score{color:var(--primary)} .summary.s3 .score{color:#0e6027}
  .summary .slab{font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink-subtle);margin-bottom:6px}
  .summary .headline{font-weight:400;font-size:19px;line-height:1.3}
  .grid{margin-top:24px;display:grid;gap:16px;grid-template-columns:repeat(auto-fill,minmax(250px,1fr))}
  .tile{display:flex;flex-direction:column;gap:11px;min-height:152px;position:relative}
  .tile.soon{background:var(--canvas);opacity:.75}
  .thead{display:flex;flex-direction:column;gap:3px}
  .tname{font-weight:600;font-size:16px}
  .tsub{margin:0}
  .status{font-weight:600;font-size:13px;display:inline-flex;align-items:center;align-self:flex-start;padding:3px 8px}
  .status.idle,.status.running{color:var(--ink-subtle);padding-left:0}
  .status.named{color:var(--cited-fg);background:var(--cited-bg)}
  .status.missed{color:var(--missing-fg);background:var(--missing-bg)}
  .status.absent{color:var(--ink-muted);background:#e8e6df}
  .status.error{color:var(--partial-fg);background:var(--partial-bg);font-size:12px}
  .meta{margin:0}
  .toggle{align-self:flex-start;background:none;border:0;padding:0 0 1px;cursor:pointer;font-family:var(--sans);
    font-size:13px;color:var(--primary);border-bottom:1px solid var(--hairline)}
  .body{font-size:14px;line-height:1.5;white-space:pre-wrap;max-height:220px;overflow:auto;color:var(--ink-muted);
    background:var(--field);border:1px solid var(--hairline);padding:12px}
  .cites-head{font-size:10px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink-subtle);margin-bottom:5px}
  .cites-list{margin:0;padding-left:18px;display:flex;flex-direction:column;gap:3px}
  .cites-list li{font-size:12.5px;line-height:1.35}
  .cite{color:var(--primary);text-decoration:none;word-break:break-word}
  .cite:hover{text-decoration:underline}
  .soonnote{font-size:13px;color:var(--ink-subtle)}
  .mx-badge{display:inline-flex;align-items:center;gap:6px;font-size:11px;letter-spacing:.06em;
    text-transform:uppercase;color:var(--ink-muted);padding:5px 10px;border:1px solid var(--hairline);align-self:flex-start}
  .mx-badge .dot{width:6px;height:6px;border-radius:50%;background:var(--primary)}
  /* email gate */
  .tile.is-gated .status,.tile.is-gated .meta,.tile.is-gated .toggle,.tile.is-gated .body,.tile.is-gated .cites{
    filter:blur(9px);pointer-events:none;user-select:none}
  .summary.is-gated .score,.summary.is-gated .headline{filter:blur(10px);user-select:none}
  .gatebox{margin-top:22px;border:1px solid var(--primary);background:var(--layer);padding:22px 24px}
  .gatebox h3{margin:0 0 6px;font-weight:600;font-size:17px}
  .gatebox p{margin:0 0 16px;font-size:14px;color:var(--ink-muted)}
  .gate-row{display:grid;grid-template-columns:1fr 1fr 1fr auto;gap:10px;align-items:end}
  @media(max-width:820px){.gate-row{grid-template-columns:1fr}}
  .gate-err{margin-top:10px;font-size:13px;color:var(--error)}
  /* calculator */
  .calc-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:28px}
  @media(max-width:820px){.calc-grid{grid-template-columns:1fr}}
  .calc-cell label{display:flex;flex-direction:column;gap:2px;font-size:13px;color:var(--ink);margin-bottom:7px}
  .calc-cell .letter{font-size:22px;font-weight:600;color:var(--primary);line-height:1}
  .calc-cell .hint{font-size:12px;color:var(--ink-subtle);font-weight:300}
  .calc-out{margin-top:26px;display:grid;grid-template-columns:repeat(3,1fr);gap:16px}
  @media(max-width:820px){.calc-out{grid-template-columns:1fr}}
  .out-card{background:var(--layer);border:1px solid var(--hairline);padding:20px}
  .out-card .olabel{font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink-subtle)}
  .out-card .onum{font-size:32px;font-weight:300;letter-spacing:-.5px;margin:8px 0 4px;font-variant-numeric:tabular-nums}
  .out-card .odetail{font-size:12.5px;color:var(--ink-muted);line-height:1.5}
  .formula-echo{margin-top:18px;font-size:13px;color:var(--ink-muted);background:var(--layer);
    border:1px solid var(--hairline);padding:12px 14px;font-variant-numeric:tabular-nums;overflow-x:auto}
  .calc-foot{margin-top:16px;font-size:12px;color:var(--ink-subtle)}
  /* proof */
  .proof-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-top:28px}
  @media(max-width:820px){.proof-grid{grid-template-columns:1fr}}
  .proof-quote{font-size:17px;line-height:1.5;font-style:italic;border-left:3px solid var(--primary);
    padding-left:16px;margin:14px 0}
  .proof-label{font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:var(--ink-subtle)}
  .proof-links{margin-top:14px;display:flex;gap:18px;flex-wrap:wrap}
  .proof-links a{font-size:14px;color:var(--primary);text-decoration:none;border-bottom:1px solid var(--hairline)}
  .proof-links a:hover{border-color:var(--primary)}
  /* ladder */
  .ladder-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:28px}
  @media(max-width:820px){.ladder-grid{grid-template-columns:1fr}}
  .ladder-step{display:flex;flex-direction:column;gap:10px}
  .ladder-step .rung{font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--primary);font-weight:600}
  .ladder-step h3{margin:0;font-weight:600;font-size:18px}
  .ladder-step p{margin:0;font-size:14px;color:var(--ink-muted);line-height:1.55}
  .roo{margin-top:22px;padding:16px 18px;background:var(--layer);border-left:3px solid var(--primary);
    font-size:14.5px;color:var(--ink)}
  /* method note */
  .method{background:var(--layer);border-top:1px solid var(--hairline);border-bottom:1px solid var(--hairline)}
  .tier-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:26px}
  @media(max-width:820px){.tier-grid{grid-template-columns:1fr}}
  .tier h4{margin:0 0 8px;font-weight:600;font-size:15px}
  .tier p{margin:0;font-size:13.5px;color:var(--ink-muted);line-height:1.55}
  .ground-rules{margin-top:20px;font-size:14px;color:var(--ink-muted);max-width:72ch}
  .stat-cite{margin-top:16px;font-size:13px;color:var(--ink-subtle);line-height:1.6}
  .stat-cite a{color:var(--primary)}
  details.faq{border-bottom:1px solid var(--hairline);padding:14px 0}
  details.faq summary{cursor:pointer;font-weight:500;font-size:15px;list-style:none;display:flex;
    justify-content:space-between;gap:12px}
  details.faq summary::after{content:"+";color:var(--primary);font-weight:400}
  details.faq[open] summary::after{content:"–"}
  details.faq p{margin:10px 0 0;font-size:14px;color:var(--ink-muted);line-height:1.6}
  /* dark band */
  .upsell{background:var(--dark);color:#f4f4f4}
  .upsell .mx-eyebrow{color:#93b4f5}
  .upsell .mx-acc-line{background:#93b4f5}
  .upsell .mx-h2{color:#fff}
  .upsell p{color:#c6c6c6;max-width:54ch;margin:18px 0 28px;font-size:18px}
  /* footer */
  footer{background:var(--canvas);border-top:1px solid var(--hairline);padding:40px 0}
  footer .frow{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:16px}
  footer .fnote{margin-top:18px;color:var(--ink-subtle);font-size:11px;line-height:1.7}
  /* ── Type — Inter throughout (matches the original demo page's font style guide exactly) ── */
  .mx-eyebrow,.mx-btn,.status,.toggle{font-weight:300}
  @media (prefers-reduced-motion: reduce){
    .ticker-track,.ticker-dot{animation:none}
    html{scroll-behavior:auto}
  }
</style>
{#- Organization + FAQPage schema — this page practises what it preaches.
    All strings pass through | dump | safe so autoescaping can't corrupt the JSON. -#}
{%- set faqItems = demo.faq or [
  { "q": "Are these AI answers live or pre-recorded?",
    "a": "Live. Every tile is a fresh call to the engine's official API the moment the test runs — the run stamp shows the exact query time. If the backend is unreachable the page shows clearly-labelled sample data instead, never passed off as live." },
  { "q": "What does the visibility score mean?",
    "a": "It counts how many of the tested answer engines named " + demo.client_name + " in their response to the query, out of the engines queried. It is a per-query spot check, not a market-wide share measurement — the full audit covers a broader query set." },
  { "q": "Is the value calculator a revenue attribution claim?",
    "a": "No. It is a modelled estimate with every coefficient visible and adjustable. Your market size and income figures come from your records — we never guess them — and the influence rate is agreed at kickoff, never asserted." },
  { "q": "How is my actual AI exposure measured?",
    "a": "The eXposure figure in the calculator is measured in your audit: a standard set of buyer-intent queries is run across the engines and a citation is recorded only as a direct link or verbatim quote. The free snapshot gives you a first read." }
] %}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Organization",
      "@id": "https://maxifidigital.com/#organization",
      "name": "Maxifi Digital",
      "url": "https://maxifidigital.com",
      "logo": "https://ai.maxifidigital.com/images/maxifi-digital-logo-black-transparent.png",
      "description": {{ "Maxifi Digital is a global Answer Engine Optimisation (AEO) consultancy helping organisations get cited by AI answer engines such as ChatGPT, Claude, Gemini, Perplexity and Google AI Overviews." | dump | safe }},
      "sameAs": [ {{ demo.case_links.live_hub | dump | safe }} ]
    },
    {
      "@type": "FAQPage",
      "mainEntity": [
        {%- for f in faqItems %}
        {
          "@type": "Question",
          "name": {{ f.q | dump | safe }},
          "acceptedAnswer": { "@type": "Answer", "text": {{ f.a | dump | safe }} }
        }{{ "," if not loop.last }}
        {%- endfor %}
      ]
    }
  ]
}
</script>
</head>
<body>

<header class="c-header"><div class="mx-container"><div class="bar">
  <a class="mx-logo" href="https://maxifidigital.com" target="_blank" rel="noopener">
    <img src="/images/maxifi-digital-logo-black-transparent.png" alt="Maxifi Digital" width="135" height="44" loading="eager">
  </a>
  <a class="mx-btn is-sm" data-cta="header-book" href="{{ demo.booking_url }}" target="_blank" rel="noopener">Book a 30-min audit call</a>
</div></div></header>

{#- §1b · citation ticker (from config; omitted entirely when the config
    carries no real mini-audit quotes — never render an empty shell) -#}
{%- if demo.ticker_quotes and demo.ticker_quotes | length %}
<div class="ticker" aria-label="AI citation feed">
  <span class="ticker-tag"><span class="ticker-dot"></span>{{ demo.ticker_tag or (demo.client_name + " · AI Visibility") }}</span>
  <div class="ticker-viewport"><div class="ticker-track">
    {%- for set in [1, 2] %}
    <span class="tk-set" {% if set == 2 %}aria-hidden="true"{% endif %}>
      {%- for t in demo.ticker_quotes %}
      <span class="ticker-item"><span class="tk-q">&ldquo;{{ t.quote }}&rdquo;</span>
        {%- if t.cited %}<span class="tk-cite">✓ {{ t.engine }}</span>
        {%- else %}<span class="tk-miss">✗ {{ t.engine }}</span>{% endif %}</span>
      <span class="ticker-sep">◆</span>
      {%- endfor %}
    </span>
    {%- endfor %}
  </div></div>
</div>
{%- endif %}

{#- §1 · Hero -#}
<section class="hero"><div class="hero-glow" aria-hidden="true"></div><div class="mx-container">
  <span class="sector-chip">{{ demo.sector_label }}</span>
  {#- The hero heading uses .mx-h2 metrics — same as the original demo page's hero,
      which sets its headline in mx-h2, not mx-display. -#}
  <h1 class="mx-h2" style="max-width:900px">{{ demo.client_name }}:<br>This is what AI tells your buyers — <span class="mx-italic" style="color:var(--primary)">live.</span></h1>
  <p class="mx-body" style="max-width:560px;margin:24px 0 0;font-size:18px">ChatGPT, Claude, Gemini, Perplexity and Google's AI Overviews are already answering your buyers' questions — word for word, with or without you. See it happen, then see what it's worth.</p>
  <div class="cta-row" style="margin-top:34px">
    <a class="mx-btn" data-cta="hero-run-test" href="#live-test">Run a live test</a>
    <a class="mx-btn is-secondary" data-cta="hero-book" href="{{ demo.booking_url }}" target="_blank" rel="noopener">Book a 30-min audit call</a>
  </div>
</div></section>

{#- §2 · Explainer video (poster + click-to-load iframe; never blocks first paint) -#}
<section class="mx-section" id="explainer" style="padding-top:38px"><div class="mx-container">
  <div class="mx-eyebrow"><span class="mx-acc-line"></span>Two minutes on how this works</div>
  <h2 class="mx-h2" style="margin-top:12px">The Visibility Value Model, explained.</h2>
  <div class="video-shell" id="video-shell">
    <button class="video-poster" id="video-poster" type="button" aria-label="Play the Visibility Value Model explainer">
      <span class="play" aria-hidden="true">▶</span>
      <h3>Watch the explainer</h3>
      <p>The MAXIFI equation, the Momentum tier, and why AI answers now decide B2B shortlists. Captions always on.</p>
    </button>
  </div>
  <p class="cap-note">Captions are always visible. Prefer numbers to narration? <a href="#calculator" style="color:var(--primary)">Skip straight to the calculator ↓</a></p>
</div></section>

{#- §3 · Live test (same probe mechanics as the original demo; email gate at the reveal) -#}
<section class="mx-section" id="live-test"><div class="mx-container">
  <div class="mx-eyebrow"><span class="mx-acc-line"></span>Live test</div>
  <h2 class="mx-h2" style="margin-top:12px">See what the engines say — right now.</h2>
  <p class="mx-body" style="max-width:560px;margin-top:14px">Enter a brand and a real buyer question. We send it verbatim to each engine's official API and show you exactly what comes back — cited or not, in its own words.</p>

  <div class="mx-card panel">
    <div class="row">
      <div><label class="fld" for="brand">Brand / organisation</label>
        <input type="text" id="brand" value="{{ demo.brand_default }}" autocomplete="off"></div>
      <div><label class="fld" for="query">Buyer-intent query</label>
        <input type="text" id="query" value="{{ demo.query_default }}" autocomplete="off"></div>
    </div>
    <div class="presets" id="presets"></div>
    <div class="actions">
      <button class="mx-btn run" id="run" type="button">Run visibility test</button>
      <label class="safe"><input type="checkbox" id="safe"> Safe demo (sample data)</label>
      <span class="tokwrap" id="tokwrap" hidden>
        <span class="fld" style="margin:0">Token</span>
        <input type="text" id="token" placeholder="access token" autocomplete="off"></span>
    </div>
    <div class="samplebar" id="samplebar" hidden>⚠ SAMPLE DATA — illustrative results, not a live engine run. Untick "Safe demo" (with API keys configured) to query the engines for real.</div>
    <div class="banner" id="banner" hidden></div>
    <div class="notice" id="notice" hidden></div>
  </div>

  <div class="runmeta" id="runmeta" hidden></div>

  <div class="summary" id="summary" hidden>
    <div class="score" id="score">–</div>
    <div><div class="slab" id="scorelab">Visibility score</div><div class="headline" id="headline"></div></div>
  </div>

  {#- email gate: appears with results; unblurs the gated engines + score -#}
  <div class="gatebox" id="gatebox" hidden>
    <h3>Three more engines + your visibility score are in.</h3>
    <p>Two results are open below. Unlock Gemini, Perplexity and AI Overviews — plus the score — and we'll email you the full result.</p>
    <form id="gate-form" name="demo-gate" method="POST" data-netlify="true" netlify-honeypot="bot-field" action="/">
      <input type="hidden" name="form-name" value="demo-gate">
      <input type="hidden" name="page" value="/demo/{{ demo.slug }}/">
      <p style="display:none"><label>Don't fill this in: <input name="bot-field"></label></p>
      <div class="gate-row">
        <div><label class="fld" for="g-name">Name</label><input type="text" id="g-name" name="name" required autocomplete="name"></div>
        <div><label class="fld" for="g-email">Work email</label><input type="email" id="g-email" name="email" required autocomplete="email"></div>
        <div><label class="fld" for="g-company">Company</label><input type="text" id="g-company" name="company" required autocomplete="organization"></div>
        <button class="mx-btn" type="submit" id="gate-submit">Email me the full result</button>
      </div>
      {#- what consent_notice_version: "demo-gate-v1" refers to. Bump both together. -#}
      <p class="gate-consent mx-meta">By requesting your result you agree we may email you about it.</p>
      <div class="gate-err" id="gate-err" hidden></div>
    </form>
  </div>

  <div class="grid" id="grid">
    <div class="tile mx-card" data-engine="openai"><div class="thead"><span class="tname">ChatGPT</span><span class="tsub mx-meta">OpenAI · GPT-4o</span></div><div class="status idle">Idle</div><div class="meta mx-meta"></div><button class="toggle" type="button" hidden>Show response</button><div class="body" hidden></div><div class="cites"></div></div>
    <div class="tile mx-card" data-engine="anthropic"><div class="thead"><span class="tname">Claude</span><span class="tsub mx-meta">Anthropic · Sonnet</span></div><div class="status idle">Idle</div><div class="meta mx-meta"></div><button class="toggle" type="button" hidden>Show response</button><div class="body" hidden></div><div class="cites"></div></div>
    <div class="tile mx-card" data-engine="google" data-gated><div class="thead"><span class="tname">Gemini</span><span class="tsub mx-meta">Google · Gemini</span></div><div class="status idle">Idle</div><div class="meta mx-meta"></div><button class="toggle" type="button" hidden>Show response</button><div class="body" hidden></div><div class="cites"></div></div>
    <div class="tile mx-card" data-engine="perplexity" data-gated><div class="thead"><span class="tname">Perplexity</span><span class="tsub mx-meta">Perplexity · Sonar</span></div><div class="status idle">Idle</div><div class="meta mx-meta"></div><button class="toggle" type="button" hidden>Show response</button><div class="body" hidden></div><div class="cites"></div></div>
    <div class="tile mx-card" data-engine="aio" data-gated><div class="thead"><span class="tname">AI Overviews</span><span class="tsub mx-meta">Google · Search</span></div><div class="status idle">Idle</div><div class="meta mx-meta"></div><button class="toggle" type="button" hidden>Show response</button><div class="body" hidden></div><div class="cites"></div></div>
    <div class="tile mx-card soon"><div class="thead"><span class="tname">Copilot</span><span class="tsub mx-meta">Microsoft</span></div><span class="mx-badge"><span class="dot"></span>Rolling out</span><div class="soonnote">No real-time API yet — arriving via our SERP layer.</div></div>
  </div>
</div></section>

{#- §4 · MAXIFI calculator -#}
<section class="mx-section" id="calculator" style="background:var(--layer);border-top:1px solid var(--hairline);border-bottom:1px solid var(--hairline)"><div class="mx-container">
  <div class="mx-eyebrow"><span class="mx-acc-line"></span>The MAXIFI equation</div>
  <h2 class="mx-h2" style="margin-top:12px">What is AI visibility worth to {{ demo.client_name }}?</h2>
  <p class="mx-body" style="max-width:600px;margin-top:14px">Six coefficients, all visible, all adjustable. Change any number and the model recalculates — nothing is hidden in the maths.</p>

  <div class="calc-grid" id="calc">
    <div class="mx-card calc-cell"><label for="c-m"><span class="letter">M</span> Market <span class="hint">buyers making this decision each year</span></label>
      <input type="number" id="c-m" min="0" step="1"></div>
    <div class="mx-card calc-cell"><label for="c-a"><span class="letter">A</span> Ask AI <span class="hint">% of them who ask an AI engine</span></label>
      <input type="number" id="c-a" min="0" max="100" step="1"></div>
    <div class="mx-card calc-cell"><label for="c-x"><span class="letter">X</span> eXposure <span class="hint">% of AI answers citing you — measured in your audit</span></label>
      <input type="number" id="c-x" min="0" max="100" step="1"></div>
    <div class="mx-card calc-cell"><label for="c-i1"><span class="letter">I</span> Influence <span class="hint">% impact on decisions</span></label>
      <input type="number" id="c-i1" min="0" max="100" step="1"></div>
    <div class="mx-card calc-cell"><label for="c-f"><span class="letter">F</span> Finish <span class="hint">% win rate once influenced</span></label>
      <input type="number" id="c-f" min="0" max="100" step="1"></div>
    <div class="mx-card calc-cell"><label for="c-i2"><span class="letter">I</span> Income <span class="hint">value of one decision</span></label>
      <input type="number" id="c-i2" min="0" step="100"></div>
  </div>

  <div class="calc-grid" style="grid-template-columns:repeat(2,1fr);margin-top:16px">
    <div class="mx-card calc-cell"><label for="c-xc">Competitor exposure <span class="hint">% of AI answers citing your competitors instead</span></label>
      <input type="number" id="c-xc" min="0" max="100" step="1"></div>
    <div class="mx-card calc-cell"><label for="c-fee">Engagement fee <span class="hint">for the break-even test</span></label>
      <input type="number" id="c-fee" min="0" step="100"></div>
  </div>

  <div class="formula-echo mx-num" id="formula-echo" aria-live="polite"></div>

  <div class="calc-out">
    <div class="out-card"><div class="olabel">Influenced value / year</div>
      <div class="onum mx-num" id="out-value">–</div>
      <div class="odetail">Revenue your current AI exposure influences each year, at these settings.</div></div>
    <div class="out-card"><div class="olabel">Revenue at risk / year</div>
      <div class="onum mx-num" id="out-risk">–</div>
      <div class="odetail" id="out-risk-detail">Value influenced by the citation gap between competitor exposure and yours.</div></div>
    <div class="out-card"><div class="olabel">Break-even</div>
      <div class="onum mx-num" id="out-breakeven">–</div>
      <div class="odetail" id="out-breakeven-detail"></div></div>
  </div>

  <p class="calc-foot">Modelled estimate, all coefficients visible — not an attribution claim.</p>
</div></section>

{#- §5 · Proof block — real citation wins from our live reference hub -#}
<section class="mx-section" id="proof"><div class="mx-container">
  <div class="mx-eyebrow"><span class="mx-acc-line"></span>Proof, not promises</div>
  <h2 class="mx-h2" style="margin-top:12px">We did this for a global trade show. In eight weeks.</h2>
  <p class="mx-body" style="max-width:600px;margin-top:14px">We built an independent reference hub for a major aviation industry event and tracked every engine monthly. The first audit, 2 July 2026:</p>
  <div class="proof-grid">
    <div class="mx-card">
      <div class="proof-label">Microsoft Copilot · buyer discovery query</div>
      <p class="proof-quote">Copilot's AI answer is built from hub pages — the hub is the #1 result on Bing, above the event's official site.</p>
      <p class="mx-body" style="font-size:13.5px">Asked where to find session summaries and speaker insights for the event, Copilot assembled its answer from the hub we built — outranking the organiser's own domain.</p>
    </div>
    <div class="mx-card">
      <div class="proof-label">Perplexity · same query</div>
      <p class="proof-quote">&ldquo;the best starting point&rdquo;</p>
      <p class="mx-body" style="font-size:13.5px">Perplexity cites the hub as &ldquo;the best starting point&rdquo; for the same buyer discovery query, with direct source links to hub pages.</p>
    </div>
  </div>
  <p class="mx-meta" style="margin-top:14px;text-transform:none;letter-spacing:0">Quotes re-typeset from engine output for legibility; verify them on the live pages below.</p>
  <div class="proof-links">
    <a href="{{ demo.case_links.live_hub }}" data-cta="proof-hub" target="_blank" rel="noopener">See the live hub →</a>
    <a href="{{ demo.case_links.citation_report }}" data-cta="proof-report" target="_blank" rel="noopener">Read the monthly citation report →</a>
  </div>
</div></section>

{#- §6 · Engagement ladder -#}
<section class="mx-section" id="ladder" style="background:var(--layer);border-top:1px solid var(--hairline);border-bottom:1px solid var(--hairline)"><div class="mx-container">
  <div class="mx-eyebrow"><span class="mx-acc-line"></span>How to start</div>
  <h2 class="mx-h2" style="margin-top:12px">Three rungs. The first one is free.</h2>
  <div class="ladder-grid">
    <div class="mx-card ladder-step"><span class="rung">1 · Free</span>
      <h3>Visibility Snapshot</h3>
      <p>A first read of your AI exposure on the queries that matter — the X in your MAXIFI equation, measured, not guessed.</p>
      <a class="mx-btn is-secondary is-sm" data-cta="ladder-snapshot" href="{{ demo.snapshot_url }}" target="_blank" rel="noopener" style="align-self:flex-start;margin-top:auto">Get the free snapshot</a></div>
    <div class="mx-card ladder-step"><span class="rung">2</span>
      <h3>Visibility Report</h3>
      <p>50+ real buyer queries across every engine, benchmarked against your competitors, with a prioritised roadmap to get cited.</p></div>
    <div class="mx-card ladder-step"><span class="rung">3</span>
      <h3>Visibility Engine</h3>
      <p>We build and run the citation programme. At the default settings above, the pilot pays for itself if it tips one-third of one decision your way — run your own numbers in the calculator.</p></div>
  </div>
  <div class="roo"><strong>Return on objectives, in writing:</strong> success is defined as citation-share movement on an agreed query set — written into the engagement.</div>
</div></section>

{#- §7 · Method note + FAQ -#}
<section class="mx-section method" id="method"><div class="mx-container">
  <div class="mx-eyebrow"><span class="mx-acc-line"></span>FAQs</div>
  <h2 class="mx-h2" style="margin-top:12px">We refuse AVE-style magic numbers.</h2>
  <div class="tier-grid">
    <div class="mx-card tier"><h4>Measured</h4><p>Actual citations captured — direct links or verbatim quotes, recorded per engine, per query, per month.</p></div>
    <div class="mx-card tier"><h4>Modelled</h4><p>Ranges, assumptions visible. Every coefficient in the value model is on the page and adjustable — nothing asserted.</p></div>
    <div class="mx-card tier"><h4>Momentum</h4><p>Authority that compounds — named, never priced. We tell you where it's building; we don't put a fake number on it.</p></div>
  </div>
  <p class="ground-rules"><strong>Ground rules:</strong> your market and income figures come from your records — we never guess them; the influence rate is agreed at kickoff, never asserted.</p>
  <p class="stat-cite">Why this matters now: 94% of B2B buyers use generative AI in the purchase process, and 54% call AI chatbots the single biggest influence on their shortlist (<a href="https://www.forrester.com/" target="_blank" rel="noopener">Forrester, January 2026</a>). Meanwhile only ~1% of users click the links inside AI answers (<a href="https://www.pewresearch.org/" target="_blank" rel="noopener">Pew Research Center, 2025</a>) — if the answer doesn't name you, the click never happens.</p>

  <h3 style="margin:36px 0 6px;font-weight:600;font-size:17px">Frequently asked questions</h3>
  {%- for f in faqItems %}
  <details class="faq"><summary>{{ f.q }}</summary><p>{{ f.a }}</p></details>
  {%- endfor %}
</div></section>

{#- §8 · Final dual CTA -#}
<section class="upsell mx-section"><div class="mx-container">
  <div class="mx-eyebrow"><span class="mx-acc-line"></span>Next step</div>
  <h2 class="mx-h2" style="margin-top:14px">Find out what AI says when you're not in the room.</h2>
  <p>Start with the free snapshot, or bring your questions to a 30-minute audit call — we'll run your real buyer queries live on the call.</p>
  <div class="cta-row">
    <a class="mx-btn is-on-dark" data-cta="final-snapshot" href="{{ demo.snapshot_url }}" target="_blank" rel="noopener">Get the free snapshot</a>
    <a class="mx-btn is-secondary" data-cta="final-book" href="{{ demo.booking_url }}" target="_blank" rel="noopener" style="color:#fff;border-color:rgba(255,255,255,.45)">Book a 30-min audit call</a>
  </div>
</div></section>

<footer><div class="mx-container">
  <div class="frow">
    <a class="mx-logo" href="https://maxifidigital.com" target="_blank" rel="noopener">
      <img src="/images/maxifi-digital-logo-black-transparent.png" alt="Maxifi Digital" width="135" height="44" loading="lazy"></a>
    <span class="mx-meta">© 2026 Maxifi Digital · Built for the answer-engine era</span>
  </div>
  <div class="fnote">This page practises what it preaches: it carries FAQPage and Organization schema.org markup — view source, or run it through a rich-results validator.<br>
  Live across ChatGPT, Claude, Gemini, Perplexity and Google AI Overviews; Copilot rolling out via our SERP layer. &nbsp; maxifidigital.com · checkyourvisibility.maxifidigital.com</div>
</div></footer>

<script>
// Per-prospect config, injected at build time from demos/{slug}.json.
const CFG = {{ demo | dump | safe }};

const LIVE_ENGINES = ["openai", "anthropic", "google", "perplexity", "aio"];
const FREE_ENGINES = ["openai", "anthropic"];   // visible before the email gate
const SAFE_DEFAULT = false;
// Demo-scoped, rotatable token; /demo/probe is proxied server-side to the engine
// backend (see netlify.toml), so no API keys and no cross-origin calls here.
const DEMO_TOKEN = "{{ env.demoToken }}";  /* injected at build from DEMO_PAGE_TOKEN — never hardcode a spendable token in a public repo */
const PROBE_URL  = "/demo/probe";

const $ = (s, r=document) => r.querySelector(s);
const $$ = (s, r=document) => Array.from(r.querySelectorAll(s));

/* ── analytics: privacy-respecting, no external calls by default.
   Events land in window.dataLayer (picked up by GTM/GA if ever added) and in
   GoatCounter if its script is present. Events: test_run, gate_pass,
   video_play, calc_use, cta_click. ── */
function mxTrack(name, props){
  try{
    (window.dataLayer = window.dataLayer || []).push(Object.assign({event:name}, props || {}));
    if (window.goatcounter && window.goatcounter.count)
      window.goatcounter.count({ path: name, title: JSON.stringify(props || {}), event: true });
  }catch(e){}
}
document.addEventListener("click", (e) => {
  const a = e.target.closest("[data-cta]");
  if (a) mxTrack("cta_click", { label: a.getAttribute("data-cta") });
});

/* ── §2 explainer video: click-to-load iframe, poster first (static-first) ── */
const EXPLAINER_SRC = CFG.explainer_src || "/demo-assets/visibility-value-explainer.html";
$("#video-poster").addEventListener("click", async () => {
  mxTrack("video_play", { slug: CFG.slug });
  const shell = $("#video-shell"), poster = $("#video-poster");
  try {
    const head = await fetch(EXPLAINER_SRC, { method: "HEAD" });
    if (!head.ok) throw new Error("missing");
  } catch (e) {
    poster.querySelector("h3").textContent = "Explainer coming soon";
    poster.querySelector("p").textContent = "The video walkthrough is being finalised — the calculator below covers the same model, interactively.";
    return;
  }
  const frame = document.createElement("iframe");
  frame.src = EXPLAINER_SRC;
  frame.title = "Visibility Value Model explainer";
  frame.allow = "fullscreen";
  frame.addEventListener("load", () => {
    // Same-origin: rebind the explainer's final "Calculate Your Visibility
    // Value" CTA to scroll to this page's calculator instead of swapping views.
    try {
      const doc = frame.contentDocument;
      $$("a,button", doc).forEach((el) => {
        if (/calculate\s+your\s+visibility\s+value/i.test(el.textContent || "")) {
          el.onclick = null;   // the explainer binds its view-swap via the onclick property
          el.addEventListener("click", (ev) => {
            ev.preventDefault(); ev.stopImmediatePropagation();
            document.getElementById("calculator").scrollIntoView({ behavior: "smooth" });
          }, true);
        }
      });
    } catch (e) {}
  });
  poster.remove();
  shell.appendChild(frame);
});

/* ── §3 live tester — same mechanics as the original single-client demo ── */
const params = new URLSearchParams(location.search);
let urlToken = params.get("token") || "";
if (!urlToken && !DEMO_TOKEN) $("#tokwrap").hidden = false;
function getToken(){ return urlToken || $("#token").value.trim() || DEMO_TOKEN; }

const urlSafe = params.get("safe");
$("#safe").checked = urlSafe != null ? (urlSafe === "1" || urlSafe === "true") : SAFE_DEFAULT;
function syncSampleBar(){ $("#samplebar").hidden = !$("#safe").checked; }
$("#safe").addEventListener("change", syncSampleBar);
syncSampleBar();

function showNotice(msg){ const el = $("#notice"); el.textContent = msg; el.hidden = false; }
function hideNotice(){ const el = $("#notice"); el.textContent = ""; el.hidden = true; }
function showBanner(msg){ const el = $("#banner"); el.textContent = msg; el.hidden = false; }
function hideBanner(){ const el = $("#banner"); el.textContent = ""; el.hidden = true; }
function shorten(s){ s = String(s||""); return s.length > 90 ? s.slice(0,90)+"…" : s; }

(CFG.preset_queries || []).forEach((p) => {
  const b = document.createElement("button");
  b.className = "chip"; b.type = "button"; b.textContent = p;
  b.addEventListener("click", () => { $("#query").value = p; });
  $("#presets").appendChild(b);
});

function tileFor(engine){ return $('.tile[data-engine="'+engine+'"]'); }
function setStatus(tile, kind, label){
  const s = $(".status", tile); s.className = "status " + kind; s.textContent = label;
}
function _domainOf(u){
  try { return new URL(u).hostname.replace(/^www\./, ""); } catch (e){ return String(u); }
}
function renderCites(box, citations){
  box.textContent = "";
  const list = (citations || []).filter(u => typeof u === "string");
  if (!list.length) return;
  const head = document.createElement("div");
  head.className = "cites-head"; head.textContent = "Sources (" + list.length + ")";
  box.appendChild(head);
  const ol = document.createElement("ol"); ol.className = "cites-list";
  list.forEach(u => {
    const li = document.createElement("li");
    const ok = /^https?:\/\//i.test(u);
    const el = document.createElement(ok ? "a" : "span");
    el.className = "cite"; el.textContent = _domainOf(u);
    if (ok){ el.href = u; el.target = "_blank"; el.rel = "noopener noreferrer"; el.title = u; }
    li.appendChild(el); ol.appendChild(li);
  });
  box.appendChild(ol);
}
function resetTile(tile){
  setStatus(tile, "running", "Asking…");
  $(".meta", tile).textContent = "";
  const body = $(".body", tile); body.hidden = true; body.textContent = "";
  $(".cites", tile).textContent = "";
  $(".toggle", tile).hidden = true;
}
function renderTile(engine, d){
  const tile = tileFor(engine); if (!tile) return;
  const meta = $(".meta", tile), body = $(".body", tile);
  const cites = $(".cites", tile), toggle = $(".toggle", tile);
  body.textContent = ""; cites.textContent = ""; meta.textContent = "";
  body.hidden = true; toggle.hidden = true;
  if (!d || (!d.ok && d.error)){
    setStatus(tile, "error", "⚠ " + shorten(d && d.error ? d.error : "Unavailable"));
    if (d && d.ms) meta.textContent = d.ms + " ms";
    return;
  }
  if (d.named){ setStatus(tile, "named", "✓ Cited"); }
  else if (d.present === false && engine === "aio"){ setStatus(tile, "absent", "— No AI Overview"); }
  else { setStatus(tile, "missed", "✗ Not cited"); }
  meta.textContent = (d.model || engine) + " · " + (d.ms || 0) + " ms" + (d.sample ? " · sample" : "");
  body.textContent = d.verbatim_response || "(empty response)";
  renderCites(cites, d.citations);
  if (d.verbatim_response){ toggle.hidden = false; toggle.textContent = "Show response"; }
}
$("#grid").addEventListener("click", (e) => {
  if (!e.target.classList.contains("toggle")) return;
  const body = $(".body", e.target.closest(".tile"));
  body.hidden = !body.hidden;
  e.target.textContent = body.hidden ? "Show response" : "Hide response";
});
// Score out of the engines that actually responded, so an engine outage
// reads as "n of N responding" rather than silently deflating the score.
function renderSummary(named, responding, brand){
  const el = $("#summary");
  const total = LIVE_ENGINES.length;
  if (!responding){
    el.hidden = true;
    showNotice("No engines responded on this run — that's a connection issue, not a visibility verdict. Try again in a moment.");
    return;
  }
  el.hidden = false;
  $("#score").textContent = named + " / " + responding;
  $("#scorelab").textContent = responding < total
    ? "Visibility score · " + responding + " of " + total + " engines responding"
    : "Visibility score";
  const qual = responding < total ? " responding" : "";
  let cls, msg;
  if (named === 0){ cls = "s0"; msg = "Invisible — not one" + qual + " engine cited " + brand + ". This is the gap."; }
  else if (named <= responding/2){ cls = "s1"; msg = "Barely visible — AI cites " + brand + " on only " + named + " of " + responding + qual + " engines."; }
  else if (named < responding){ cls = "s2"; msg = "Partial visibility — AI cites " + brand + " on some" + qual + " engines but misses you on others."; }
  else { cls = "s3"; msg = "Strong visibility — every" + qual + " engine cited " + brand + ". Now defend that lead."; }
  el.classList.remove("s0","s1","s2","s3"); el.classList.add(cls);
  $("#headline").textContent = msg;
}
function setRunMeta(sample){
  const el = $("#runmeta");
  if (sample){ el.className = "runmeta sample"; el.textContent = "Sample data — illustrative, not a live run."; }
  else {
    const ts = new Date().toISOString().replace("T", " ").slice(0, 19) + " UTC";
    el.className = "runmeta live"; el.textContent = "● LIVE · queried " + ts;
  }
  el.hidden = false;
}

/* ── email gate: 2 engines free; the rest + the score blur until the form
   passes. Passing persists in localStorage so returning visitors stay open. ── */
const GATE_KEY = "mx-demo-gate-passed";
function gatePassed(){ try { return localStorage.getItem(GATE_KEY) === "1"; } catch(e){ return false; } }
function applyGate(){
  if (gatePassed()) return;
  $$(".tile[data-gated]").forEach(t => t.classList.add("is-gated"));
  $("#summary").classList.add("is-gated");
  $("#gatebox").hidden = false;
}
function liftGate(){
  $$(".tile[data-gated]").forEach(t => t.classList.remove("is-gated"));
  $("#summary").classList.remove("is-gated");
  $("#gatebox").hidden = true;
}
$("#gate-form").addEventListener("submit", async (e) => {
  e.preventDefault();
  const btn = $("#gate-submit"), err = $("#gate-err");
  err.hidden = true; btn.disabled = true; btn.textContent = "Sending…";
  const fd = new FormData(e.target);
  const data = new URLSearchParams(fd);
  data.set("brand", $("#brand").value.trim());
  data.set("query", $("#query").value.trim());

  /* Two independent sinks, so a lead is lost only if both fail:
     1. /demo/capture — the engine's lead store (via a Netlify Function that
        holds the bearer token server-side). Carries the run, so a follow-up
        knows what the prospect actually saw.
     2. Netlify Forms — the original POST, kept as the fallback. */
  const email = (fd.get("email") || "").toString().trim();
  const r = LAST_RUN;
  // Sample output is never sent as a measurement (CLAUDE.md: never claim a
  // measurement the engine cannot take). A sample run still captures the lead.
  const measured = !!r && !r.sample && r.responding > 0;

  let captured = false;
  try {
    const res = await fetch("/demo/capture", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        slug: CFG.slug,
        source: "demo_gate",
        consent: true,
        consent_notice_version: "demo-gate-v1",
        dedupe_key: CFG.slug + ":" + email.toLowerCase(),
        run_date: new Date().toISOString().slice(0, 10),
        email,
        contact_name: (fd.get("name") || "").toString().trim() || null,
        brand: $("#brand").value.trim() || null,
        timezone: Intl.DateTimeFormat().resolvedOptions().timeZone || null,
        top_competitors: CFG.competitor_names || [],
        /* probe_results ONLY — the engine rejects 422 "pass either probe_results
           or explicit metrics, not both". It derives named_rate itself and
           deliberately leaves visibility_score null (probe-one carries no rank or
           sentiment, so it refuses to guess a full score). Sending our own would
           claim a measurement the engine declines to make. */
        probe_results: measured ? r.results : null
      })
    });
    captured = res.ok;
  } catch (e) {}

  let delivered = false;
  try {
    const res = await fetch("/", { method: "POST",
      headers: { "Content-Type": "application/x-www-form-urlencoded" }, body: data.toString() });
    delivered = res.ok;
  } catch (e) {}
  // Unlock either way — a form-endpoint hiccup shouldn't strand a prospect —
  // but tell them only if BOTH sinks failed and we genuinely have nothing.
  try { localStorage.setItem(GATE_KEY, "1"); } catch (e) {}
  mxTrack("gate_pass", { slug: CFG.slug, delivered, captured });
  liftGate();
  if (!captured && !delivered){
    showNotice("Results unlocked. The emailed copy could not be sent just now — book a call or use the snapshot link and we'll send it manually.");
  }
  btn.disabled = false; btn.textContent = "Email me the full result";
});

/* ── run ── */
/* Last completed run, hoisted out of run()'s local scope so the email gate can
   send the measurement alongside the lead. Stays null until a run finishes, and
   carries `sample` so illustrative output is never captured as measured. */
let LAST_RUN = null;

async function probeEngine(engine, payload){
  const res = await fetch(PROBE_URL, {
    method: "POST",
    headers: { "Content-Type": "application/json", "X-Demo-Token": payload.token || "" },
    body: JSON.stringify({ brand: payload.brand, query: payload.query, engine, token: payload.token, safe: payload.safe })
  });
  if (res.status === 401){ const e = new Error("auth"); e.code = 401; throw e; }
  if (!res.ok){ throw new Error("http " + res.status); }
  return res.json();
}
async function run(){
  hideBanner(); hideNotice();
  $("#runmeta").hidden = true;
  const brand = $("#brand").value.trim();
  const query = $("#query").value.trim();
  const token = getToken();
  const safe = $("#safe").checked;
  if (!brand || !query){ showBanner("Enter a brand name and a query."); return; }
  if (!token){ showBanner("Enter the demo access token to run live tests."); return; }
  mxTrack("test_run", { slug: CFG.slug, safe });

  const btn = $("#run"); btn.disabled = true; btn.textContent = "Running…";
  $("#summary").hidden = true;
  LIVE_ENGINES.forEach(e => { const t = tileFor(e); if (t) resetTile(t); });

  let auth = false, capped = false, capMsg = "", sample = false, named = 0;
  const settled = await Promise.all(LIVE_ENGINES.map(async (engine) => {
    try { return { engine, d: await probeEngine(engine, { brand, query, token, safe }) }; }
    catch (err){ return { engine, err }; }
  }));
  settled.forEach(({ engine, d, err }) => {
    if (err){
      if (err.code === 401) auth = true;
      renderTile(engine, { ok:false, error: err.code === 401 ? "Invalid demo token" : "Network error" });
      return;
    }
    if (d.capped){ capped = true; capMsg = d.error; }
    if (d.sample){ sample = true; }
    renderTile(engine, d);
    if (d.ok && d.named) named++;
  });

  const okCount = settled.filter(s => s.d && s.d.ok).length;
  if (auth){ showBanner("Invalid or missing demo token — check your link."); }
  else if (capped){ showBanner(capMsg || "Daily demo budget reached."); }
  else if (okCount === 0){ renderSummary(0, 0, brand); }   // no responders: notice, no score, no LIVE stamp
  else { renderSummary(named, okCount, brand); setRunMeta(sample); applyGate(); }
  if (sample){ showNotice("Safe demo mode — illustrative sample data, not live engine results."); $("#samplebar").hidden = false; }

  LAST_RUN = {
    brand, query, sample,
    named, responding: okCount,
    /* The frozen six-field probe-one shape the engine's summarize_probe_results()
       reads. Responding engines only: it derives named_rate from the list length,
       so including a failed engine would silently dilute the rate — same rule the
       on-page score already follows ("responding engines only"). */
    results: settled.filter(s => s.d && s.d.ok).map(s => ({
      engine: s.engine,
      named: !!s.d.named,
      verbatim_response: s.d.verbatim_response || "",
      citations: s.d.citations || [],
      model: s.d.model || null,
      ms: s.d.ms ?? null
    }))
  };

  btn.disabled = false; btn.textContent = "Run visibility test";
  return { okCount, auth, capped };
}
$("#run").addEventListener("click", run);
$("#query").addEventListener("keydown", (e) => { if (e.key === "Enter") run(); });
$("#brand").addEventListener("keydown", (e) => { if (e.key === "Enter") run(); });

/* Clearly-labelled illustrative fallback if the backend is unreachable —
   generic wording only; never presented as a live result. */
function renderSample(){
  hideBanner();
  const brand = $("#brand").value.trim() || "the brand";
  renderSummary(2, 4, brand);   // the illustrative set has 4 responding tiles (one shows an error)
  setRunMeta(true);
  showNotice("Illustrative sample — the live engine backend was unreachable, so these are example results, not a live run.");
  $("#samplebar").hidden = false;
  renderTile("openai",{ok:true,named:true,model:"sample",ms:1180,citations:[],
    verbatim_response:"[Illustrative sample] An engine response naming " + brand + " would appear here, word for word."});
  renderTile("anthropic",{ok:true,named:false,present:true,model:"sample",ms:1520,citations:[],
    verbatim_response:"[Illustrative sample] A response that fails to mention " + brand + " would appear here — that's the gap this page measures."});
  renderTile("google",{ok:false,ms:870,error:"sample: backend unreachable"});
  renderTile("perplexity",{ok:true,named:true,model:"sample",ms:1340,
    citations:[CFG.case_links && CFG.case_links.live_hub ? CFG.case_links.live_hub : "https://maxifidigital.com"],
    verbatim_response:"[Illustrative sample] A cited response with live source links would appear here."});
  renderTile("aio",{ok:true,named:false,present:false,model:"AI Overviews",ms:980,citations:[],verbatim_response:""});
  applyGate();
}
// One real run on load so the first view is genuine engine output — skipped when
// the config ships no default brand (e.g. the generic /demo/b2b page), so the
// generic page never spends engine budget by itself.
async function autoRun(){
  if (!(CFG.brand_default || "").trim() || !(CFG.query_default || "").trim()) return;
  try { const r = await run(); if (!r || r.auth || r.okCount === 0) renderSample(); }
  catch (e) { renderSample(); }
}
autoRun();

/* ── §4 MAXIFI calculator — seeded from config value_defaults ── */
const V = CFG.value_defaults || {};
const CUR = CFG.currency_symbol || "$";
const seed = {
  m: V.M != null ? V.M : 100,
  a: (V.A != null ? V.A : 0.5) * 100,
  x: (V.X != null ? V.X : 0.05) * 100,
  i1: (V.I_influence != null ? V.I_influence : 0.05) * 100,
  f: (V.F != null ? V.F : 1) * 100,
  i2: V.I_income != null ? V.I_income : 20000,
  xc: (V.X_comp != null ? V.X_comp : 0.5) * 100,
  fee: V.fee != null ? V.fee : 4500
};
Object.entries(seed).forEach(([k, v]) => { $("#c-" + k).value = v; });

function money(n){
  return CUR + Math.round(n).toLocaleString("en-GB");
}
let calcUsed = false;
function calc(){
  const n = (id) => { const v = parseFloat($("#c-" + id).value); return isFinite(v) ? Math.max(v, 0) : 0; };
  const M = n("m"), A = n("a")/100, X = Math.min(n("x")/100, 1), I1 = n("i1")/100,
        F = n("f")/100, I2 = n("i2"), XC = Math.min(n("xc")/100, 1), FEE = n("fee");

  const value = M * A * X * I1 * F * I2;
  const gap = Math.max(XC - X, 0);
  const risk = M * A * gap * I1 * F * I2;

  $("#formula-echo").textContent =
    "M " + M.toLocaleString("en-GB") + " × A " + (A*100).toFixed(0) + "% × X " + (X*100).toFixed(0) +
    "% × I " + (I1*100).toFixed(0) + "% × F " + (F*100).toFixed(0) + "% × I " + money(I2) +
    " = " + money(value) + " influenced value / year";
  $("#out-value").textContent = money(value) + "/yr";
  $("#out-risk").textContent = money(risk) + "/yr";
  $("#out-risk-detail").textContent = gap > 0
    ? "Citation gap of " + (gap*100).toFixed(0) + " points between competitor exposure (" + (XC*100).toFixed(0) + "%) and yours (" + (X*100).toFixed(0) + "%)."
    : "No citation gap at these settings — your exposure meets or beats the competitor figure.";

  if (I2 > 0 && risk > 0){
    const decisions = FEE / I2;
    const pct = (FEE / risk) * 100;
    $("#out-breakeven").textContent = decisions.toFixed(2) + " decisions";
    $("#out-breakeven-detail").textContent =
      "The fee of " + money(FEE) + " equals " + decisions.toFixed(2) + " of one decision — and " +
      pct.toFixed(0) + "% of the revenue at risk each year.";
  } else if (risk <= 0){
    $("#out-breakeven").textContent = "—";
    $("#out-breakeven-detail").textContent = "No citation gap at these settings — there is no at-risk revenue to break even against.";
  } else {
    $("#out-breakeven").textContent = "—";
    $("#out-breakeven-detail").textContent = "Set the value of one decision to model break-even.";
  }
}
$$("input[id^='c-']").forEach((el) => el.addEventListener("input", () => {
  if (!calcUsed){ calcUsed = true; mxTrack("calc_use", { slug: CFG.slug }); }
  calc();
}));
calc();
</script>
{%- if demo.analytics_goatcounter %}
<script data-goatcounter="https://{{ demo.analytics_goatcounter }}.goatcounter.com/count"
        async src="https://gc.zgo.at/count.js"></script>
{%- endif %}
</body>
</html>
