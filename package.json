{
  "name": "maxifi-pages",
  "version": "1.0.0",
  "description": "ai.maxifidigital.com — lead-gen landing page + per-prospect AI-visibility demo pages (/demo-{slug}).",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rimraf _site"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "rimraf": "^6.1.3"
  }
}
